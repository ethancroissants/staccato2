# Staccato — the project README lives at the repo root: ../README.md

See the top-level `../README.md` for the architecture overview, how the
notation & fingering modules work, the Spark color system, and accessibility notes.

Quick start from this directory:

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # production bundle in dist/
```
